package com.comp2042.system_events;

/**
 * Defines the direction determined by a keystroke or from the system's automatic processing.
 */
public enum EventType {
    DOWN, LEFT, RIGHT, ROTATE_CLOCK, ROTATE_ANTICLOCK, SNAP
}
